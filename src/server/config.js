export function loadDotEnv(file='.env') { try { const text = requireEnvFile(file); for (const line of text.split(/\r?\n/)) { const m=line.match(/^\s*([A-Za-z_][A-Za-z0-9_]*)\s*=\s*(.*)\s*$/); if(m && process.env[m[1]]===undefined) process.env[m[1]]=m[2].replace(/^['"]|['"]$/g,''); } } catch {} }
function requireEnvFile(file){ const fs = globalThis.__fsForConfig; if(fs) return fs.readFileSync(file,'utf8'); throw new Error('no sync fs'); }
export function configFromEnv(){ return { port:Number(process.env.PORT||3000), geminiApiKey:process.env.GEMINI_API_KEY||'', geminiModel:process.env.GEMINI_MODEL||'gemini-3.7-flash', maxPromptChars:Number(process.env.MAX_PROMPT_CHARS||30000), maxContextChars:Number(process.env.MAX_CONTEXT_CHARS||60000), allowedOrigins:(process.env.ALLOWED_ORIGINS||'').split(',').map(x=>x.trim()).filter(Boolean), timeoutMs:Number(process.env.GEMINI_TIMEOUT_MS||30000), estimatedJudgeMs:Number(process.env.GEMINI_ESTIMATED_MS||8000), pqaApiToken:process.env.PQA_API_TOKEN||'', protectAnalysis:/^(1|true|yes)$/i.test(process.env.PQA_PROTECT_ANALYSIS||'false') }; }
